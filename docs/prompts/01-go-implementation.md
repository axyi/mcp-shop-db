# Prompt 01 — go implementation

**Stage:** implementation (`go docs/spec/spec-v0.md`)

## Prompt sent

```
go docs/spec/spec-v0.md
```

Per `AGENTS.md`'s "go protocol", this is a standing instruction to execute
`docs/spec/spec-v0.md` end-to-end following its own Execution contract
(section 1): create the files listed in section 2, follow the tests-first
implementation order of section 4, run the acceptance gates of section 6
verbatim, and report per section 7.

## Outcome

All four gates passed on the first gate run (0 repair-and-rerun cycles used).
See `docs/reports/` for the run report.
